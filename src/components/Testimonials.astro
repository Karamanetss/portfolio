---
// 👇 Определяем интерфейс для одного элемента, как вы и сделали
interface Testimonial {
  id: number;
  text: string;
  author: string;
}

// 👇 Создаем специальный интерфейс `Props`, который Astro сам поймет
interface Props {
  testimonials: Testimonial[];
  title: string;
  subtitle: string;
}

const { testimonials = [], title = '', subtitle = '' } = Astro.props;
---

<section id="testimonials" class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-5">
    <div
      class="text-3xl md:text-[36px] md:text-start text-center mb-15 text-black font-light"
      data-aos="fade-up"
    >
      <p
        class="text-2xl sm:text-3xl mb-2 text-black font-light text-center md:text-start"
      >
        {title}
      </p>
      <h2
        class="text-4xl sm:text-5xl lg:text-6xl xl:text-[90px] md:text-start text-center font-bold font-montserrat text-black"
      >
        {subtitle}
      </h2>
    </div>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/keen-slider@6.8.5/keen-slider.min.css"
    />
    <div data-aos="fade-up" class="keen-slider" id="testimonials-slider">
      {
        testimonials.map((item) => (
          <div class="keen-slider__slide flex flex-col items-center px-6">
            <span class="text-5xl text-black flex justify-center w-full ">
              &ldquo;
            </span>
            <p class="text-lg text-black font-light text-center max-w-2xl mb-6">
              {item.text}
            </p>
            <hr class="w-16 border-black mb-2" />
            <p class="font-montserrat font-bold text-black text-lg">
              {item.author}
            </p>
          </div>
        ))
      }
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/keen-slider@6.8.5/keen-slider.min.js"
    ></script>
    <script is:inline>
      document.addEventListener('DOMContentLoaded', function () {
        const slider = document.querySelector('#testimonials-slider');
        if (slider) {
          new KeenSlider(slider, {
            loop: true,
            slides: {
              perView: 1,
              spacing: 15,
            },
            breakpoints: {
              '(min-width: 640px)': {
                slides: { perView: 2, spacing: 15 },
              },
              '(min-width: 1024px)': {
                slides: { perView: 3, spacing: 15 },
              },
            },
          });
        }
      });
    </script>
  </div>
</section>
